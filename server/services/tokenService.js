// TODO: JWT token service
// generateAuthToken()     - Sign JWT with user id + role (expiry: e.g. 1 day)
// generateDownloadToken() - Sign single-use JWT for secure PDF download (expiry: 7 days)
// verifyToken()           - Verify and decode JWT
